import React, { Component } from 'react';
import Styles from './styles.scss';
import PropTypes from 'prop-types';

export default class Toogle extends Compoments {
    render () {

        return (
            <section className = { Styles.toggle }>
                <button>

                </button>
            </section>
        );
    }
}